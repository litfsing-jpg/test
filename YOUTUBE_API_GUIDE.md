# 🎥 Инструкция: Сбор реальных данных с YouTube API

## 📋 Шаг 1: Получение YouTube API Key

### 1.1 Создание проекта в Google Cloud Console

1. Откройте: **https://console.cloud.google.com/**
2. Войдите в свой Google аккаунт
3. Нажмите **"Select a project"** (вверху страницы) → **"New Project"**
4. Введите название проекта, например: **"Fitness Bloggers Data"**
5. Нажмите **"Create"**

### 1.2 Включение YouTube Data API v3

1. В левом меню выберите: **"APIs & Services"** → **"Library"**
2. В поиске введите: **"YouTube Data API v3"**
3. Нажмите на **"YouTube Data API v3"**
4. Нажмите **"Enable"** (Включить)

### 1.3 Создание API ключа

1. В левом меню выберите: **"APIs & Services"** → **"Credentials"**
2. Нажмите **"Create Credentials"** (вверху) → **"API Key"**
3. Скопируйте созданный ключ (он выглядит примерно так: `AIzaSyDxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)
4. ✅ Сохраните этот ключ в безопасном месте!

### 1.4 (Опционально) Ограничение ключа

Для безопасности рекомендуется ограничить ключ:

1. Нажмите **"Restrict Key"**
2. В разделе **"API restrictions"** выберите **"Restrict key"**
3. Выберите только **"YouTube Data API v3"**
4. Нажмите **"Save"**

---

## 🚀 Шаг 2: Установка зависимостей

```bash
pip install requests
```

---

## 🔑 Шаг 3: Настройка API ключа

### Вариант 1: Через файл (Рекомендуется)

Создайте файл `.youtube_api_key` в папке проекта:

```bash
echo "AIzaSyDxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" > .youtube_api_key
```

Замените `AIzaSyDxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` на ваш реальный ключ.

### Вариант 2: Через переменную окружения

```bash
export YOUTUBE_API_KEY="AIzaSyDxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
```

---

## ▶️ Шаг 4: Запуск скрипта

```bash
python3 collect_youtube_data.py
```

### Что делает скрипт:

1. ✅ Читает файл `fitness_trainers_viral.csv`
2. ✅ Находит все YouTube каналы
3. ✅ Для каждого канала:
   - Получает реальное количество подписчиков
   - Находит последние 10 Shorts
   - Считает просмотры
   - Рассчитывает вирусный коэффициент (просмотры/подписчики)
4. ✅ Сохраняет результат в `fitness_trainers_viral_real.csv`

### Пример вывода:

```
📊 Найдено YouTube каналов: 26
📊 Других платформ: 166
⏳ Начинаю сбор данных...

[1/26] Anna Tsukur
   URL: https://youtube.com/@AnnaTsukur
   ✅ ID: UC1234567890
   👥 Подписчики: 71.4K
   🎬 Найдено Shorts: 8
   📊 Средние просмотры: 150K
   🔥 Коэффициент: 2.10x
   ✅ Обновлено! Использовано квоты: 206/10000
```

---

## 📊 Шаг 5: Обновление сайта

После успешного сбора данных:

```bash
# Заменить старый файл на новый с реальными данными
mv fitness_trainers_viral_real.csv fitness_trainers_viral.csv

# Закоммитить изменения
git add fitness_trainers_viral.csv
git commit -m "Обновлены данные YouTube каналов с реальными метриками из API"
git push origin claude/fitness-bloggers-landing-011CUvVSayX8qzLHSEqTrLmp
```

Через 1-2 минуты сайт автоматически обновится!

---

## ⚠️ Важные ограничения

### Квота API (Бесплатный план)

- **Дневной лимит**: 10,000 единиц
- **Стоимость запросов**:
  - Получение статистики канала: **3 единицы**
  - Поиск канала по @username: **100 единиц**
  - Получение списка видео: **100 единиц**
  - Получение деталей видео: **3 единицы**

**Итого на 1 канал**: ~206 единиц

**Максимум каналов в день**: ~48 каналов

### Рекомендации:

1. **Запускайте скрипт постепенно** - обрабатывайте по 20-30 каналов в день
2. **Кэшируйте данные** - не обновляйте всё сразу, только устаревшие данные
3. **Следите за квотой** - скрипт автоматически останавливается при достижении 90% лимита

---

## 🔄 Автоматическое обновление

Для автоматического обновления данных каждую неделю можно настроить:

### GitHub Actions (бесплатно)

Создайте файл `.github/workflows/update-youtube-data.yml`:

```yaml
name: Update YouTube Data

on:
  schedule:
    # Запускать каждое воскресенье в 00:00 UTC
    - cron: '0 0 * * 0'
  workflow_dispatch: # Ручной запуск

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Collect YouTube data
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: python3 collect_youtube_data.py

      - name: Update CSV
        run: mv fitness_trainers_viral_real.csv fitness_trainers_viral.csv

      - name: Commit and push
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add fitness_trainers_viral.csv
          git commit -m "Автообновление данных YouTube $(date +'%Y-%m-%d')"
          git push
```

Не забудьте добавить **API ключ в секреты GitHub**:
1. Settings → Secrets and variables → Actions
2. New repository secret
3. Name: `YOUTUBE_API_KEY`
4. Value: ваш API ключ

---

## 📈 Что вы получите

### До (Фейковые данные):
```
Anna Tsukur - 150K подписчиков (неверно!)
Просмотры: случайные числа
Коэффициент: сгенерирован случайно
```

### После (Реальные данные из API):
```
Anna Tsukur - 71.4K подписчиков ✅
Просмотры: реальные данные из последних 10 Shorts
Коэффициент: рассчитан на основе реальных просмотров
```

---

## 🆘 Устранение проблем

### Ошибка: "API key not valid"
- Проверьте, что API включен в Google Cloud Console
- Убедитесь, что ключ скопирован полностью
- Подождите 5-10 минут после создания ключа

### Ошибка: "Quota exceeded"
- Подождите до следующего дня (квота обновляется в 00:00 PST)
- Или обновите до платного плана

### Ошибка: "Channel not found"
- Проверьте формат URL (должен быть youtube.com/@username)
- Убедитесь, что канал существует и публичен

---

## 💡 Дополнительные возможности

Скрипт можно расширить для:
- ✅ Instagram Graph API (требует Business аккаунт)
- ✅ TikTok Research API (только для исследователей)
- ✅ Telegram Bot API (публичные каналы)
- ✅ VK API (требует токен приложения)

Нужна помощь с другими платформами? Дайте знать!
